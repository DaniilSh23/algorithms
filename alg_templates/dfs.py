"""
DFS (Поиск в глубину)
Условия: "Все пути", "связные компоненты"
Задачи LeetCode: #46 (Permutations), #78 (Subsets), #79 (Word Search)
"""

def dfs(node):
    if node in visited:
        return
    visited.add(node)
    for neighbor in graph[node]:
        dfs(neighbor)


# Или с возвратом (backtracking):
def backtrack(path):
    if len(path) == target:
        result.append(path[:])
        return
    for choice in candidates:
        path.append(choice)
        backtrack(path)
        path.pop()


"""
Не разбирал практическое применение кода выше, кстати, этот код написал Grok AI.

Но вот, что обсудил с другой нейронкой, касательно общей логики алгоритма DFS:

Мой вопрос:
окей, а логика DFS  - это когда мы на старте выбираем одно из направлений и пробуем по нему пройти до конца. 
Затем выбираем следующее направление и тоже пробуем дойти до конца. Как правило DFS реализуется с помощью рекурсии ?


Да, логика **DFS** как раз такая:

- **В начале вы выбираете одно направление** (одного из соседей стартовой клетки) и максимально идёте по этому пути, 
пока либо не дойдёте до финиша, либо не упрётесь в тупик.
- Затем **возвращаетесь назад** (например, к предыдущей клетке) и пробуете другой вариант, снова проходя по нему до конца.
- Так постепенно перебираются все возможные пути, **каждый раз погружаясь глубоко**, пока не будут исследованы все варианты.

**DFS очень часто реализуют с помощью рекурсии**, потому что это автоматический способ "запоминать" текущий путь и 
возвращаться назад (как будто шаги раскручиваются обратно).  
Также можно реализовать DFS через стек — по сути это тоже способ хранить "куда мы дошли" для возврата назад.

**То есть:**  
- DFS берёт маршрут "вглубь" — до конца,
- возвращается назад, если не удалось пройти,
- перебирает так все направления,
- обычно используется рекурсия (или стек).

**В задачах наподобие поиска пути в матрице, такой метод позволит найти любой путь (или все пути), но не обязательно 
самый короткий первым.**

[1](https://leetcode.com/problems/shortest-path-in-binary-matrix/description/)
"""